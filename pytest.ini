[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests (no IO, no network)
    api: API endpoint tests (TestClient)
    integration: Integration tests (real Redis/PG)
    slow: Slow tests (skip in CI quick runs)
