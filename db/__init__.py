"""
Database layer — SQLAlchemy models, connection, repositories.

Phase 2 implementation. Currently stubbed with in-memory fallbacks.
When PostgreSQL is configured (DATABASE_URL in .env), real DB is used.
"""
